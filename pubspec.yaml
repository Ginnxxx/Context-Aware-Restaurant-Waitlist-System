name: queueless
description: "QueueLess - a context-aware restaurant waitlist system."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.17.1
  geolocator: ^14.0.3
  native_geofence: ^1.3.1
  permission_handler: 12.0.1
  qr_flutter: ^4.1.0
  mobile_scanner: ^7.4.0
  firebase_core: ^4.13.0
  firebase_messaging: ^16.5.0
  flutter_map: ^8.3.2
  latlong2: ^0.10.1
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
